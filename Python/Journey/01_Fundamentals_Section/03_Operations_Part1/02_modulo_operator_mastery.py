# Modulo Operator Challenge - Mastery Challenge

# Integer division uses the // operator, which divides and
# discards the remainder (returns only the integer part).
# Example: 7 // 2 = 3 (not 3.5)

# Challenge (Beginner):
# Write a code that initializes three variables, x, y, and z
# with the values 15, 4, and 23 (respectively).
#
# After that, initialize:
# w = x // y
# v = z // x
# u = z // y
#
# Observe how different dividends and divisors affect the results.

# Type your code below
x = 15
y = 4
z = 23

w = x // y
v = z // x
u = z // y

# Don't change the lines below
print(f"x = {x}")
print(f"y = {y}")
print(f"z = {z}")
print(f"w = {w}")
print(f"v = {v}")
print(f"u = {u}")
